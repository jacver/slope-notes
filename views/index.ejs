

<head>
    <%- include('./partials/head'); %>
</head>

<body class="container">
<main>
  <div class="card">
  <div class=" list-group  page-title-container">
  <h1 class="page-title">
    My Resorts
  </h1>
    <form class="dropdown" action="" method="">
      <select  class="btn-primary btn-add" title="Add Resort"> 
        <option class="dropdown-option0 .lead"value="0"> 
          Add Resort:
        </option>
        <% resorts.forEach((resort, index) => { %>
          <option class=".lead" value="<%= resort.index %>">
            <%= resort.resortName %>
          </option>
          <% }) %>
      </select>
    </form>
</div>
  <% resorts.forEach(resort => { %>
  <div class="card-body">
    <div class="card-titles-container">
     <h2 class="card-title"><%= resort.resortName %></h2>
      <h3 class="card-subtitle subtitle mb-2 text-muted"><%= resort.resortState %>, <%= resort.resortCountry %></h3>
    </div>
    <div class="card-buttons-container">
      <a href="/<%=resort.id%>" >
        <button title="View Resort" class="btn btn-primary">
          View
        </button>
      </a>

      <form
      method="POST"
      action="/<%=resort.id%>?_method=DELETE"
        class="removeFormMargin"
      >
        <input class="btn btn-secondary" type="submit"  
        title="Delete Resort"      value="Delete" />
      </form>
    </div>
  </div>


  <% }) %>






  </main>

    <footer><%- include('./partials/footer'); %></footer>
  </body>
</html>
